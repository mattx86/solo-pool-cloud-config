# Tari Pool Wallet Configuration
# Note: gRPC is enabled via command-line flag --enable-grpc
# Network is set via command-line flag --network

[wallet]
# Valid wallet settings for current Tari version
base_node_rpc_pool_size = 5

[wallet.p2p]
# Transport for P2P communication
transport = "tcp"
